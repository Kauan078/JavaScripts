// * operadores ternários, é  com um if else curto, nescessario ? como if e : como else
// é  com um if else curto, nescessario ? como if e : como elseele é mais util para retornar valores

const idade = 12;

// idade >= 18 ? console.log("Maior de idade") : console.log("Voce é menor de idade")


const mensagens =  idade >= 18 ? "Maior de idade" : "Menor de idade";